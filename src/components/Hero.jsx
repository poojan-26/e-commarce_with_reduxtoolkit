export default function Hero({ page }) {
  // START RENDER //
  return (
    <section className="page-hero">
      <div className="section-center">
        <h3 className="page-hero-title">{page}</h3>
      </div>
    </section>
  );
  // END RENDER //
}
